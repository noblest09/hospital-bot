services:
  - type: web
    name: hospital-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python hospital_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
